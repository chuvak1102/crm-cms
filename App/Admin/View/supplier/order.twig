{% extends 'layout.twig' %}
{% block content %}
    <div class="wrapper wrapper-content  animated fadeInRight">
        <div class="row">
            <div class="col-lg-12">
                <div class="ibox">
                    <div class="ibox-content">
                        <div class="table-responsive">
                            <table class="table table-hover issue-tracker checkbox-container">
                                <tbody>
                                <tr>
                                    <th>ID</th>
                                    <th>Дата</th>
                                    <th>Статус</th>
                                    <th>Цена</th>
                                    <th>Поставщик</th>
                                </tr>
                                {% for i in order %}
                                    <tr>
                                        <td><a href="/supplier/order/edit/{{ i.id }}">{{ i.id }}</a></td>
                                        <td>{{ i.created | date('d-m-Y H:i') }}</td>
                                        <td>{{ i.getStatus.name }}</td>
                                        <td>{{ i.total }}</td>
                                        <td>{{ i.getSupplier.name }}</td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}